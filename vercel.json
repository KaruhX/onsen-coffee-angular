{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist/frontend/browser/* public/",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.py"
    }
  ]
}
